spring:
  datasource:
    driver-class-name: org.mariadb.jdbc.Driver
    url: jdbc:mariadb://localhost/jpa1
    username: root
    password: green502
  jpa:
    hibernate:
      ddl-auto: update #update #create 하면 서버 켰다킬때마다 자동으로 새로 만들어 줌
    show-sql: true
    properties:
      hibernate:
       format_sql: true #??? ??

springdoc:
  swagger-ui:
    path: /swagger.html
